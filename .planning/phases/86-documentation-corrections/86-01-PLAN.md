---
phase: 86-documentation-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/docs/docs/getting-started/index.md
  - website/docs/.vitepress/config.mts
  - website/docs/.vitepress/theme/components/landing/HeroSection.vue
  - website/docs/.vitepress/theme/components/landing/GetStartedCTA.vue
autonomous: true

must_haves:
  truths:
    - "Getting-started guide uses `meshc` as the binary name everywhere (not `mesh`)"
    - "Landing page install CTA shows working `curl -sSf https://mesh-lang.org/install.sh | sh` command"
    - "Hero section version badge shows `v7.0` (not `v0.1.0`)"
    - "Getting-started description says 'compiled via LLVM' (not 'compiles through Rust')"
    - "All code examples in getting-started use project-based workflow (`meshc build .`) that works when copy-pasted"
  artifacts:
    - path: "website/docs/docs/getting-started/index.md"
      provides: "Corrected getting-started guide with working install, binary name, description, and examples"
      contains: "meshc build"
    - path: "website/docs/.vitepress/config.mts"
      provides: "Updated meshVersion config value"
      contains: "meshVersion: '7.0'"
    - path: "website/docs/.vitepress/theme/components/landing/HeroSection.vue"
      provides: "Dynamic version badge reading from theme config"
      contains: "theme.value.meshVersion"
    - path: "website/docs/.vitepress/theme/components/landing/GetStartedCTA.vue"
      provides: "Corrected install command URL with .sh extension"
      contains: "install.sh"
  key_links:
    - from: "website/docs/.vitepress/theme/components/landing/HeroSection.vue"
      to: "website/docs/.vitepress/config.mts"
      via: "useData().theme.meshVersion"
      pattern: "theme\\.value\\.meshVersion"
    - from: "website/docs/.vitepress/theme/components/landing/GetStartedCTA.vue"
      to: "website/docs/public/install.sh"
      via: "install command URL"
      pattern: "mesh-lang\\.org/install\\.sh"
---

<objective>
Fix all documentation inaccuracies in the getting-started guide and landing page so that users following the guide on a clean system can successfully install and run Mesh.

Purpose: The getting-started guide and landing page were written before the CLI was renamed, the install script was built (Phase 82), and the compiler switched to project-based compilation. Users hitting these docs today encounter wrong binary names, broken install instructions, incorrect compilation descriptions, and code examples that don't work.

Output: 4 corrected website source files covering all 5 DOCS requirements (DOCS-01 through DOCS-05).
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-documentation-corrections/86-RESEARCH.md
@website/docs/docs/getting-started/index.md
@website/docs/.vitepress/config.mts
@website/docs/.vitepress/theme/components/landing/HeroSection.vue
@website/docs/.vitepress/theme/components/landing/GetStartedCTA.vue
@website/docs/.vitepress/theme/components/docs/DocsVersionBadge.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix landing page version badge, install command, and config</name>
  <files>
    website/docs/.vitepress/config.mts
    website/docs/.vitepress/theme/components/landing/HeroSection.vue
    website/docs/.vitepress/theme/components/landing/GetStartedCTA.vue
  </files>
  <action>
    Three targeted edits to landing page components and config:

    **1. config.mts** (line 76): Change `meshVersion: '0.1.0'` to `meshVersion: '7.0'`
    This is the single source of truth for the version badge across the site. The language milestone version is v7.0 (latest git tag), NOT the Cargo.toml crate version (0.1.0).

    **2. HeroSection.vue** (line 55): Replace the hardcoded version text with a dynamic reference.
    - Current: `Now in development &mdash; v0.1.0` (hardcoded string)
    - Change to use `useData()` from VitePress to read `theme.meshVersion` dynamically, matching the pattern already used by `DocsVersionBadge.vue` (line 9 of that file: `const { theme } = useData()` then `theme.value.meshVersion`).
    - In the `<script setup>` block, add: `import { useData } from 'vitepress'` and `const { theme } = useData()`
    - In the template at line 55, replace the hardcoded text with: `Now in development &mdash; v{{ theme.meshVersion }}`
    - This ensures future version bumps only need to change config.mts.

    **3. GetStartedCTA.vue** (line 11): Fix the install command URL.
    - Current: `'curl -sSf https://mesh-lang.org/install | sh'`
    - Change to: `'curl -sSf https://mesh-lang.org/install.sh | sh'`
    - The `.sh` extension is required -- the install script is served at `mesh-lang.org/install.sh` (confirmed in `website/docs/public/install.sh` header, line 3).
  </action>
  <verify>
    - `grep 'meshVersion' website/docs/.vitepress/config.mts` shows `'7.0'`
    - `grep 'meshVersion' website/docs/.vitepress/theme/components/landing/HeroSection.vue` shows dynamic `theme.meshVersion` reference (not hardcoded `v0.1.0`)
    - `grep 'install.sh' website/docs/.vitepress/theme/components/landing/GetStartedCTA.vue` shows URL with `.sh` extension
    - No occurrences of `v0.1.0` remain in HeroSection.vue
  </verify>
  <done>
    - Config meshVersion is `'7.0'` (DOCS-03)
    - HeroSection version badge dynamically reads from config, displays `v7.0` (DOCS-03)
    - GetStartedCTA install command has correct `install.sh` URL (DOCS-02)
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite getting-started guide with correct install, binary name, description, and working examples</name>
  <files>
    website/docs/docs/getting-started/index.md
  </files>
  <action>
    Rewrite the getting-started guide to fix all remaining issues. The file has 163 lines. Specific changes:

    **DOCS-04 -- Fix compilation description (lines 11, 16, 24):**
    - Line 11: Change "native compilation via Rust" to "native compilation via LLVM"
    - Line 16: Change "Mesh compiles through Rust to produce fast native binaries" to "Mesh compiles via LLVM to produce fast native binaries"
    - Line 24: The "Mesh compiles through Rust, so you need the Rust toolchain" line is part of the old install section -- this entire section gets replaced (see below)

    **DOCS-01 + DOCS-02 -- Replace Installation section (lines 20-48):**
    Replace the entire "Installation" section (Prerequisites + Building from Source + Verifying Installation) with:

    ```markdown
    ## Installation

    Install Mesh with a single command:

    ```bash
    curl -sSf https://mesh-lang.org/install.sh | sh
    ```

    This downloads the latest `meshc` binary and adds it to your PATH.

    ### Verifying Installation

    After installing, verify the compiler is available:

    ```bash
    meshc --version
    ```

    You should see the Mesh version number printed to the terminal.
    ```

    This removes the outdated build-from-source instructions (cargo build, Rust prerequisite, wrong `cd mesh` directory name) and replaces with the `curl | sh` installer from Phase 82.

    **DOCS-01 + DOCS-05 -- Fix Hello World section (lines 50-73):**
    The current examples use `mesh hello.mpl` (single-file execution) which doesn't work -- `meshc` uses project-based compilation (`meshc build <dir>`).

    Restructure the Hello World section to use the project workflow:
    1. Use `meshc init hello` to create a project
    2. Show the `main.mpl` content (the existing `fn main() do println("Hello, World!") end` is fine)
    3. Use `meshc build .` to compile
    4. Run `./hello` to execute

    Example structure:
    ```markdown
    ## Hello World

    Create a new Mesh project:

    ```bash
    meshc init hello
    cd hello
    ```

    Open `main.mpl` -- meshc generates a starter file for you. Replace its contents with:

    ```mesh
    fn main() do
      println("Hello, World!")
    end
    ```

    Compile and run it:

    ```bash
    meshc build .
    ./hello
    ```

    You should see `Hello, World!` printed to the terminal.
    ```

    Keep the same breakdown explanation bullets (fn main, do...end, println, .mpl extension).

    **DOCS-01 + DOCS-05 -- Fix "Your First Program" section (lines 75-102):**
    Replace the `greet.mpl` single-file example with editing `main.mpl` in the existing project:
    - Instead of "Create a file called `greet.mpl`", say "Open `main.mpl` and replace its contents with:"
    - Instead of `mesh greet.mpl`, use `meshc build .` then `./hello`
    - Keep all the same code (greet function) and explanation bullets

    **DOCS-05 -- Fix "Adding More Functions" section (around line 104):**
    - No compile command is shown here (just code + explanation), so no binary name fix needed
    - The code examples themselves are valid Mesh -- leave them as-is

    **DOCS-05 -- Fix "Using the Pipe Operator" section (around line 135):**
    - No compile command shown -- code examples are valid Mesh -- leave as-is

    **General:** Ensure NO occurrences of bare `mesh ` (as a command) remain anywhere in the file. Only `meshc` should appear as a command. The word "Mesh" as a language name is fine.
  </action>
  <verify>
    - `grep -c 'mesh ' website/docs/docs/getting-started/index.md` -- check each match is the language name "Mesh" not a command; no bare `mesh hello.mpl` or `mesh greet.mpl` patterns
    - `grep 'meshc' website/docs/docs/getting-started/index.md` shows `meshc init`, `meshc build .`, `meshc --version`
    - `grep 'via LLVM' website/docs/docs/getting-started/index.md` confirms compilation description
    - `grep -c 'through Rust\|via Rust' website/docs/docs/getting-started/index.md` returns 0 (all old descriptions removed)
    - `grep -c 'cargo\|rustup\|target/release' website/docs/docs/getting-started/index.md` returns 0 (all build-from-source removed)
    - `grep 'install.sh' website/docs/docs/getting-started/index.md` shows correct install URL
  </verify>
  <done>
    - Binary name is `meshc` throughout (DOCS-01)
    - Install section uses `curl -sSf https://mesh-lang.org/install.sh | sh` (DOCS-02)
    - Description says "via LLVM" not "through Rust" (DOCS-04)
    - All code examples use project-based workflow: `meshc init`, `meshc build .`, `./binary` (DOCS-05)
    - No references to build-from-source, cargo, rustup, or target/release remain
  </done>
</task>

</tasks>

<verification>
Run these checks after both tasks complete:

1. **DOCS-01 (Binary name):** `grep -rn 'mesh ' website/docs/docs/getting-started/index.md` -- verify every match is the language name "Mesh" (capitalized, not a command)
2. **DOCS-02 (Install command):** `grep 'install.sh' website/docs/.vitepress/theme/components/landing/GetStartedCTA.vue` and `grep 'install.sh' website/docs/docs/getting-started/index.md` -- both present
3. **DOCS-03 (Version badge):** `grep "meshVersion" website/docs/.vitepress/config.mts` shows `'7.0'`; `grep "v0.1.0" website/docs/.vitepress/theme/components/landing/HeroSection.vue` returns nothing
4. **DOCS-04 (Description):** `grep -c "via LLVM" website/docs/docs/getting-started/index.md` >= 1; `grep -c "through Rust" website/docs/docs/getting-started/index.md` == 0
5. **DOCS-05 (Code examples):** `grep "meshc build" website/docs/docs/getting-started/index.md` present; `grep "mesh hello\|mesh greet" website/docs/docs/getting-started/index.md` returns nothing
</verification>

<success_criteria>
All five DOCS requirements satisfied:
- DOCS-01: `meshc` used as binary name in getting-started (zero occurrences of `mesh` as a command)
- DOCS-02: Landing page CTA and getting-started guide both show `curl -sSf https://mesh-lang.org/install.sh | sh`
- DOCS-03: Version badge shows `v7.0` via config.mts meshVersion, consumed dynamically by HeroSection.vue
- DOCS-04: Getting-started says "compiled via LLVM" (not "compiles through Rust")
- DOCS-05: All code examples use `meshc init` / `meshc build .` / `./binary` project-based workflow
</success_criteria>

<output>
After completion, create `.planning/phases/86-documentation-corrections/86-01-SUMMARY.md`
</output>
