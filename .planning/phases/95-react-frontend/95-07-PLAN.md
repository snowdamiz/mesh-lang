---
phase: 95-react-frontend
plan: 07
type: execute
wave: 4
depends_on: ["95-02", "95-03", "95-04", "95-05", "95-06"]
files_modified:
  - frontend/src/app.tsx
  - frontend/src/pages/dashboard.tsx
  - frontend/src/pages/issues.tsx
  - frontend/src/pages/events.tsx
  - frontend/src/pages/live-stream.tsx
  - frontend/src/pages/alerts.tsx
  - frontend/src/pages/settings.tsx
autonomous: false

must_haves:
  truths:
    - "All 6 pages render correctly and are navigable via sidebar"
    - "Dark and light theme toggle works across all pages"
    - "Push panel detail view works on Issues and Events pages"
    - "The entire app compiles and builds for production without errors"
    - "WebSocket status indicator is visible in the header"
  artifacts:
    - path: "frontend/dist/"
      provides: "Production build output"
  key_links:
    - from: "frontend/src/app.tsx"
      to: "All pages"
      via: "React Router routes"
      pattern: "Route.*path"
---

<objective>
Final integration pass and visual verification of the complete React frontend. Fix any compilation errors, ensure all pages work together, verify the build succeeds, and get user visual approval of the app.

Purpose: This is the final quality gate. All plans must integrate cleanly, the production build must succeed, and the user verifies the visual result matches their Linear-inspired vision.
Output: Production-ready frontend build and user approval.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-react-frontend/95-RESEARCH.md
@.planning/phases/95-react-frontend/95-01-SUMMARY.md
@.planning/phases/95-react-frontend/95-02-SUMMARY.md
@.planning/phases/95-react-frontend/95-03-SUMMARY.md
@.planning/phases/95-react-frontend/95-04-SUMMARY.md
@.planning/phases/95-react-frontend/95-05-SUMMARY.md
@.planning/phases/95-react-frontend/95-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration fixes and production build verification</name>
  <files>
    frontend/src/app.tsx
    frontend/src/pages/dashboard.tsx
    frontend/src/pages/issues.tsx
    frontend/src/pages/events.tsx
    frontend/src/pages/live-stream.tsx
    frontend/src/pages/alerts.tsx
    frontend/src/pages/settings.tsx
  </files>
  <action>
    1. Run `cd frontend && npx tsc --noEmit` and fix ALL type errors. Plans 01-06 were built in parallel (wave 2 and 3), so there may be integration issues:
       - Import path mismatches between components
       - Type mismatches in shared component props
       - Missing exports from stores or hooks
       - Duplicate type definitions

    2. Verify all route connections in app.tsx match the actual page component exports.

    3. Verify push panel integration: the PushPanelLayout in issues.tsx and events.tsx correctly renders EventDetail and IssueDetail components based on ui-store state.

    4. Verify WebSocket integration: the hook connects, dispatches to ws-store, and the Live Stream page and Dashboard both consume from the store correctly.

    5. Verify the sonner toast provider is mounted in app.tsx (needed for alert notifications and settings success messages).

    6. Run `cd frontend && npm run build` and ensure production build completes with zero errors.

    7. Run `cd frontend && npm run preview` to verify the production build serves correctly.

    8. Fix any Tailwind v4 class issues (v4 may have different class names or behaviors than v3).

    9. Verify dark/light theme toggle works on every page (no hardcoded colors leaking through).

    10. Ensure all relative time formatting is consistent across components (use a shared `formatRelativeTime` utility if not already shared).
  </action>
  <verify>
    `cd frontend && npx tsc --noEmit` passes with zero errors.
    `cd frontend && npm run build` succeeds with zero errors.
    `ls frontend/dist/index.html` exists (production build output).
  </verify>
  <done>
    All TypeScript errors resolved. Production build succeeds. All pages render, routes work, stores integrate, and theme toggle functions across the entire app.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete React frontend</name>
  <files>frontend/src/app.tsx</files>
  <action>
    Present the complete frontend for visual verification. Start dev server with `cd frontend && npm run dev`.
    User verifies: sidebar navigation, all 6 pages render, dark/light theme toggle, push panel detail views on Issues/Events, alert rule creation dialog, settings tabs, WebSocket status dot, and overall Linear-inspired monochrome aesthetic.
  </action>
  <verify>
    1. Start the frontend: `cd frontend && npm run dev`
    2. Open http://localhost:5173 in your browser
    3. Verify the sidebar shows navigation items (Dashboard, Issues, Events, Live Stream, Alerts, Settings)
    4. Click through each page and verify it renders (data will be empty without the backend running)
    5. Toggle dark/light theme and verify consistent styling
    6. Check the Issues and Events pages for push panel behavior (click a row if data available)
    7. Check the Alerts page for rule creation dialog
    8. Check the Settings page tabs
    9. Verify the WebSocket status dot in the header (should be red when no backend running)
    10. Overall: does the aesthetic match a Linear-inspired, clean, monochrome design?
  </verify>
  <done>User approves the visual design and overall functionality of the complete React frontend.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` passes
2. `cd frontend && npm run build` succeeds
3. Production build output exists in `frontend/dist/`
4. User visual approval received
</verification>

<success_criteria>
- Zero TypeScript compilation errors
- Production build succeeds
- All 6 pages render correctly
- Dark/light theme works everywhere
- Push panel pattern works on Issues and Events
- WebSocket status indicator visible
- User approves the visual design
</success_criteria>

<output>
After completion, create `.planning/phases/95-react-frontend/95-07-SUMMARY.md`
</output>
